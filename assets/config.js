/* Q-Park Fenix overlast — frontend configuratie.
   apiBase = "" betekent: gebruik dezelfde Worker als waar de site uit serveert
   (same-origin, dus relatieve URL's als /api/count). Dat is de standaard­opstelling
   in dit project: één Worker, één origin. Vul alleen iets in als de API ooit
   ergens anders draait.
*/
window.BuurtConfig = {
  apiBase: ""
};
