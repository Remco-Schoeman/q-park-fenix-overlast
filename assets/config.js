/* Buurtdossier — frontend configuration.
   Set apiBase to the URL of your deployed Cloudflare Worker (zonder slash aan het eind),
   bv. "https://buurtdossier-api.jouw-account.workers.dev".
   Laat leeg om de site in "offline" modus te draaien (counter en aansluiten staan uit).
*/
window.BuurtConfig = {
  apiBase: ""
};
